name: Sync Matrix

on:
  schedule:
    - cron: '0 * * * *' # Runs hourly
  workflow_dispatch: # Allows manual triggering of the workflow

jobs:
  call-endpoints:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        endpoint:
          - https://validators-api-mainnet.nuxt.dev
          - https://validators-api-testnet.nuxt.dev
          - https://dev.validators-api-mainnet.nuxt.dev
          - https://dev.validators-api-testnet.nuxt.dev
    steps:
      # - name: Stream sync API
      #   run: |
      #     # --no-buffer (or -N) tells curl to flush each chunk as soon as it arrives,
      #     # so can see Nitro stream lines in real time.
      #     curl -fsS --no-buffer "${{ matrix.endpoint }}/api/v1/sync/sse" || {
      #       echo "Sync request failed for ${{ matrix.endpoint }}"
      #       exit 1
      #     }
      - name: Sync missing epochs
        env:
          ENDPOINT: ${{ matrix.endpoint }}
        run: |
          set -euo pipefail
          echo "🔄 Starting missing-epoch sync at $ENDPOINT"
          # Loop until the endpoint reports isSynced: true
          while true; do
            resp=$(curl -fsS "$ENDPOINT/api/v1/sync/missing-epoch")
            echo "$resp"
            isSynced=$(echo "$resp" | jq -r '.isSynced')
            syncedEpoch=$(echo "$resp" | jq -r '.syncedEpoch')
            totalMissing=$(echo "$resp" | jq -r '.totalMissing')

            if [[ "$isSynced" == "true" && "$syncedEpoch" == "null" ]]; then
              echo "✅ All epochs already synced"
              break
            elif [[ "$isSynced" == "true" ]]; then
              echo "✅ Synced epoch $syncedEpoch - All epochs synced"
              break
            fi

            next=$(echo "$resp" | jq -r '.next')
            echo "✅ Synced epoch $syncedEpoch, next epoch is $next ($totalMissing remaining)"
          done

      - name: Sync snapshot
        env:
          ENDPOINT: ${{ matrix.endpoint }}
        run: |
          echo "🔄 Syncing snapshot at $ENDPOINT"
          curl -fsS "$ENDPOINT/api/v1/sync/snapshot" || {
            echo "❌ Snapshot sync failed for $ENDPOINT"
            exit 1
          }
